import { CalendarEvent } from "@/lib/types";

export default function CalendarGrid({ events: _events }: { events: CalendarEvent[] }) {
  return <div className="text-gray-500">Kalendervy kommer snart...</div>;
}
